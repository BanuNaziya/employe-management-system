<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>

        <!-- ===== Database Connection Settings ===== -->
        <!-- MySQL JDBC Driver class -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>

        <!-- Database URL - update 'ems_db' if you use a different database name -->
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/ems_db?useSSL=false&amp;serverTimezone=UTC&amp;allowPublicKeyRetrieval=true</property>

        <!-- Change these to your MySQL username and password -->
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password">password</property>

        <!-- ===== Hibernate Settings ===== -->
        <!-- MySQL 8 dialect - tells Hibernate to generate MySQL-compatible SQL -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQL8Dialect</property>

        <!-- auto creates or updates tables based on entity classes -->
        <!-- Use 'create' first time, then change to 'update' -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Set to true to see SQL queries in console (helpful for debugging) -->
        <property name="hibernate.show_sql">false</property>
        <property name="hibernate.format_sql">false</property>

        <!-- Connection pool size (keep small for simple projects) -->
        <property name="hibernate.connection.pool_size">5</property>

        <!-- ===== Entity Classes (mapped to database tables) ===== -->
        <mapping class="com.ems.model.User"/>
        <mapping class="com.ems.model.Employee"/>

    </session-factory>
</hibernate-configuration>
